# Herencia y Polimorfismo — Sistema Multimedia
Jerarquía `Multimedia` → `Disco` con lista propia (`ListaMultimedia`). Uso de `instanceof` para distinguir tipos en tiempo de ejecución y casting seguro.
**Conceptos:** polimorfismo, `instanceof`, casting, `toString` con `super`, `equals` sobreescrito.
