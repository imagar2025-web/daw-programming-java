# Interfaces — Vehículo
Interfaz `Vehiculo` con constantes (`VELMAXSPAIN`), métodos abstractos (`arrancar`, `acelerar`, `frenar`) y método `default`. Ejemplo de contrato que deben cumplir las clases implementadoras.
**Conceptos:** `interface`, constantes en interfaces, `default` methods, `implements`.
