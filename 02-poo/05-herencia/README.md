# Herencia — Animal, Gato y Perro
Jerarquía clásica de herencia: clase madre `Animal` con atributos comunes, y clases hijas `Gato` y `Perro` con comportamientos específicos. Uso de `super()` y sobrecarga de constructores.
**Conceptos:** `extends`, `super()`, sobrecarga de constructores, herencia de atributos y métodos.
