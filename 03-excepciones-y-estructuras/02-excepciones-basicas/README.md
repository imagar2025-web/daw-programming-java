# Excepciones Básicas
Introducción a `try-catch-finally` y creación de excepciones personalizadas con `extends Exception`.
**Conceptos:** `try-catch`, `finally`, `throw`, `throws`, excepciones custom.
